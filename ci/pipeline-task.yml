---
platform: linux

image_resource:
  type: docker-image
  source:
    #repository: runmymind/docker-android-sdk
    repository: busybox
    tag: latest
    
inputs:
  - name: git-source
    
run:
  path: ls
  args: [-lsa]
  # path: bash
  # args: [./git-source/ci/run_sq_scan.sh]